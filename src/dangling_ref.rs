pub fn dang() {
    let r = dangle();
}

pub fn dangle() {
    let f = String::from("soon will be released");
    //error: this function's return type contains a borrowed value, but there is no value for it to be borrowed from

    //&f
}